PORT=5000
JWT_SECRET=supersecuresecretkeyshouldbechangedinproduction
DB_TYPE=sqlite
# For MySQL config:
DB_HOST=localhost
DB_USER=root
DB_PASS=password
DB_NAME=company_management
DB_PORT=3306

# For MongoDB config (Data Collection Module):
# MONGODB_URI=mongodb://localhost:27017/company_management

# For Cloudinary config (Optional: if empty, local storage under uploads/ is used):
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
